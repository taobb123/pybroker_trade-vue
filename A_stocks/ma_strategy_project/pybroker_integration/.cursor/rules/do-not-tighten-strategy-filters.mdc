---
description: 未经用户主动要求，不得收紧与现有策略相抵触的筛选条件
alwaysApply: true
---

# 现有策略筛选条件：默认不动

- 六组合「下跌放量」(combo 6) 入观察池的条件保持原样：位置为 `downtrend` 即入池，**不要**再加 `match_exact=True`、真放量阴线、ST 排除等额外过滤。
- `match_exact` 只作诊断标签（精确 vs 近邻），不作为观察池 / 形态建仓 / M+ 动量池的入池门闩。
- 不要改动会改变现有动量策略股票池或风险收益结构的筛选、阈值、过期规则，除非用户**主动明确要求**修改该条件。
- 发现标签与行情「看起来不一致」时：先说明原因，提出方案等确认；不要自行收紧条件。
