# Cloudflare Workers：把 Vite 构建产物 dist 作为静态站点发布
# 用于替代无效的 `echo "deploy"`，避免一直显示 Hello world
name = "pybroker-trade-vue"
compatibility_date = "2024-01-01"
workers_dev = true

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
