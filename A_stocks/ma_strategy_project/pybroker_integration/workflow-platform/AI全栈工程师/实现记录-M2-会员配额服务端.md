# 实现记录 · M2 会员 / 配额服务端化

> 纪要：`../产品经理/协作纪要/005-MVP闭环规则-基于1.2.md`  
> 规则：`MVP闭环规则-v1.md`

## 交付

| 项 | 路径 |
|----|------|
| 表 | `quota_ledger` · `payments`（`db.py`） |
| 服务 | `membership_service.py`（plan 过期回落、日配额、bonus） |
| API | `/api/quota/today` · `/api/quota/consume` · `/api/membership/me` · `set-free` · `bonus` |
| 支付 | `payment_api.py`：下单需登录；`simulate-pay` → `apply_membership` |
| 前端 | `stores/quota.ts` / `billing.ts` 调服务端；支付后 `auth.bootstrap` + `quota.refresh` |

## 验收

- [x] 登录后配额来自服务端  
- [x] 运行发起调用 `/api/quota/consume`，失败不本地假扣  
- [x] Pro 模拟支付后刷新仍为 Pro（memberships 持久化）  
- [x] 订单列表来自 `/api/payment/orders`  
- [x] 切 Free 写服务端  

## 下一刀

M3 Onboarding：首次引导 + 当日 +3 bonus（限制仅首次）。
