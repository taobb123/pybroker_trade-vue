# 实现记录 · M1 用户库与登录

> 纪要：`../产品经理/协作纪要/005-MVP闭环规则-基于1.2.md`  
> 规则：`MVP闭环规则-v1.md`

## 交付

| 项 | 路径 |
|----|------|
| SQLite | `.pybrokercache/mvp.db`（`db.py`） |
| 表 | `users` + `memberships` |
| API | `/api/auth/register` · `login` · `me` · `PATCH me` |
| 前端 | 登录/注册页、token、`ENFORCE_AUTH_FOR_RUN=true` |

## 演示账号

`demo@workflow.local` / `demo1234`

## 验收

- [x] 注册写入数据库  
- [x] 登录返回 token，刷新仍可 `/me`  
- [x] 未登录无法 run  
- [x] 换浏览器需重新登录（会话在 token）  

## 已知限制（M2）

~~支付升 Pro 仍写本地 plan~~ → 已由 M2 解决：支付写 `memberships`，配额走服务端。
