# 实现记录 · 定向自选推送（2+3 / 4+6 优势）

## 依据

仅多头年化对比：2+3 优势在 **Q_L / B_L**；4+6 优势在 **M+_L / A_L**；**双池都推 M-**。

映射：量能≈A，估值因子≈B。

## 推送

| 步骤 | 推送分组 | 不推 |
|------|----------|------|
| `fetch_pattern_entry` | 量能、M加、M减 | Q、估值因子 |
| `vp_combo_23_long_compare` | Q、估值因子、23M减（各 Top2） | — |

## 凯利

跟随各自推送名单；Q/估值因子/23M减 用 `combo23_latest`；量能/M加/M减 用 `latest`(4+6)。
